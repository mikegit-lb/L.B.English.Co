import { styles as s, icon, link, button, eyebrow } from '../ui.js';
import { exams, familyMeta, familyExamIds } from '../exams.js';

const familyPanel = (family) => {
  const meta = familyMeta[family];
  const links = familyExamIds(family).map((id) => {
    const exam = exams[id];
    return `<a href="${exam.route}" class="group rounded-2xl border border-ink/15 bg-white/70 p-5 transition-transform motion-safe:hover:-translate-y-1"><span class="flex items-center justify-between"><strong class="text-lg">${exam.label}</strong><span class="grid h-9 w-9 place-items-center rounded-full bg-ink text-white">${icon('arrow','h-4 w-4')}</span></span><span class="mt-3 block text-sm leading-6 text-muted">${exam.skills}</span></a>`;
  }).join('');
  return `<article class="rounded-[2rem] ${meta.color==='lime'?'bg-lime/70':'bg-lilac/85'} p-6 sm:p-8"><div class="flex items-center justify-between gap-4"><p class="${s.eyebrow}">${meta.label}</p><a class="text-xs font-bold underline underline-offset-4" href="${meta.route}">See the full comparison</a></div><h2 class="mt-6 max-w-lg text-3xl font-semibold tracking-tight sm:text-4xl">${family==='international'?'International goals. Clearer preparation.':'Türkiye’s exams, each with a sharper purpose.'}</h2><p class="mt-4 max-w-xl text-sm leading-7 text-muted">${meta.intro}</p><div class="mt-7 grid gap-3">${links}</div></article>`;
};

export const home = () => ({
  page: 'index', title: 'Choose the exam. Own the next step.',
  description: 'Choose between IELTS, TOEFL iBT, SAT Reading & Writing, YDT, YDS and YÖKDİL, then turn preparation into a weekly plan you can keep.',
  body: `<section id="top" class="${s.wrap} grid gap-12 pb-16 pt-14 lg:grid-cols-[1fr_.82fr] lg:items-end lg:pt-20"><div><p class="${s.eyebrow} mb-6 flex items-center gap-2.5 text-muted"><span class="h-2 w-2 rounded-full bg-ink"></span>A smarter way to prepare</p><h1 class="max-w-3xl text-[clamp(3.1rem,6.5vw,6.7rem)] font-semibold leading-[.98] tracking-[-.075em]">Choose the exam.<br>Own the next step.<br><span class="relative inline-block">Make progress visible.</span></h1><p class="mt-8 max-w-xl text-base leading-8 text-muted">One goal deserves one clear route. Match your exam, focus your week and build the confidence to move forward.</p><div class="mt-8 flex flex-wrap gap-3">${button('Help me choose','data-open="finder"')}${link('See the free library','resources.html',true)}</div></div><div class="rounded-[2rem] bg-ink p-6 text-white sm:p-8"><p class="${s.eyebrow} text-lime">The right choice changes everything</p><p class="mt-6 text-2xl font-semibold leading-tight">IELTS and TOEFL prove English proficiency. SAT builds college readiness. YDT, YDS and YÖKDİL answer distinct requirements in Türkiye.</p><div class="mt-8 grid grid-cols-3 gap-3 border-t border-white/20 pt-6 text-xs"><span><strong class="block text-2xl text-lime">06</strong>exam routes</span><span><strong class="block text-2xl text-lime">16</strong>starter lessons</span><span><strong class="block text-2xl text-lime">01</strong>local study plan</span></div></div></section><section id="exam-routes" class="${s.wrap} grid gap-5 pb-16 lg:grid-cols-2">${familyPanel('international')}${familyPanel('turkiye')}</section><section class="${s.wrap} pb-20"><div class="grid gap-8 overflow-hidden rounded-[2rem] bg-[#e6e9dc] p-4 sm:grid-cols-[1fr_.85fr] sm:items-end sm:p-5"><img src="assets/hero-coaching.jpg" alt="An online English coaching conversation" width="1536" height="1024" class="h-64 w-full rounded-[1.4rem] object-cover sm:h-80"><div class="p-4 sm:p-7"><p class="${s.eyebrow} text-muted">Coaching that moves with you</p><h2 class="mt-5 text-3xl font-semibold leading-tight tracking-tight">Your plan should feel personal.</h2><p class="mt-4 text-sm leading-7 text-muted">Bring the deadline, the goal and the part that keeps slowing you down. We turn them into your next best move.</p>${button('Build my coaching brief','data-open="consultation"',true)}</div></div></section><section class="bg-white" id="how-it-works"><div class="${s.wrap} grid gap-10 py-20 lg:grid-cols-[.72fr_1.28fr]"><div>${eyebrow('A better week starts here')}<h2 class="${s.heading}">Know your next best move.</h2><p class="mt-5 text-sm leading-7 text-muted">Choose the goal, try the method, save what works and build a week you can actually keep.</p>${button('Build my study plan','data-open="planner"',true)}</div><div class="grid gap-5 sm:grid-cols-3">${[['Choose','Match the exam to the outcome you need.'],['Practise','Practise one high-value decision with immediate feedback.'],['Carry forward','Leave with one action you can repeat this week.']].map(([title,copy],i)=>`<article class="border-t-2 border-ink pt-5"><span class="text-xs font-bold text-muted">0${i+1}</span><h3 class="mt-5 text-xl font-semibold">${title}</h3><p class="mt-3 text-sm leading-7 text-muted">${copy}</p></article>`).join('')}</div></div></section><section id="formats" class="${s.wrap} py-20 sm:py-24">${eyebrow('Support that fits the way you work')}<h2 class="${s.heading} mt-5">Focused support for a result that matters.</h2><div class="mt-10 grid gap-4 md:grid-cols-3">${[['Private 1:1','A flexible pace, direct feedback and room to improve.'],['VIP group of 3–5','Small-group energy with practice built around your level.'],['Exam consultancy','A clear review of your route, priorities and next investment.']].map(([title,copy],i)=>`<article class="rounded-2xl border border-line p-7"><p class="text-xs font-bold text-muted">0${i+1}</p><h3 class="mt-6 text-xl font-semibold">${title}</h3><p class="my-4 text-sm leading-7 text-muted">${copy}</p>${button('Build my coaching brief',`data-open="consultation" data-format="${title}"`,true)}</article>`).join('')}</div></section><section id="resources" class="${s.wrap} pb-20"><div class="grid gap-8 rounded-[2rem] bg-[#e6e9dc] p-7 sm:p-10 lg:grid-cols-[.85fr_1.15fr] lg:items-end"><div>${eyebrow('Your first useful step is free')}<h2 class="${s.heading} mt-5">Small actions.<br>Visible progress.</h2><p class="mt-5 max-w-md text-sm leading-7 text-muted">Keep the prompts, notes and weekly plan that make your practice easier to repeat.</p>${link('Open the free library','resources.html',true)}</div><div class="grid gap-3 sm:grid-cols-2">${[['TOEFL iBT 2026+','Email checklist','toefl-notes'],['YDT','Net takip çizelgesi','ydt-net'],['YDS · YÖKDİL','Vocabulary error log','yds-log'],['SAT','Reading & Writing pacing','sat-pacing']].map(([label,title,id])=>`<a href="resources.html?resource=${id}" class="rounded-xl bg-white/75 p-5"><span class="text-[10px] font-bold tracking-widest text-muted">${label}</span><strong class="mt-2 block text-sm">${title} ${icon('diagonal','inline h-4 w-4')}</strong></a>`).join('')}</div></div></section><section id="faq" class="${s.wrap} grid gap-10 pb-24 md:grid-cols-[.72fr_1.28fr]"><div>${eyebrow('Before you begin')}<h2 class="${s.heading} mt-5">Good questions.<br>Make a confident choice.</h2></div><div>${[['Which exam gives me the right advantage?','Start with the requirement, not the name. IELTS and TOEFL assess English proficiency; SAT supports college admission; YDT, YDS and YÖKDİL serve different Türkiye requirements.'],['Is SAT an English test or a college-readiness test?','No. The full SAT includes Math and Reading & Writing. This studio’s SAT coaching covers Reading & Writing only.'],['Are these official questions or scores?','No. All starters are original practice. Feedback describes the activity you completed and does not predict an official score.'],['Can I get a plan made for me?','Yes. Choose private 1:1, a VIP group of 3–5 or exam consultancy, then prepare a brief with your goal and timeline.']].map(([q,a])=>`<details class="border-b border-line py-5"><summary class="cursor-pointer pr-5 text-sm font-bold leading-6">${q}</summary><p class="mt-4 pr-4 text-sm leading-7 text-muted">${a}</p></details>`).join('')}</div></section>`
});
