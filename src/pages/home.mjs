import { styles as s, icon, link, button, eyebrow } from '../ui.js';
import { exams, familyMeta, familyExamIds } from '../exams.js';

const homeServices = [
  ['Everyday English', 'Speak, write and understand English with more ease.', 'For real conversations, travel and the confidence to keep going.'],
  ['Work & career', 'Lead meetings, interviews and messages with clarity.', 'Practise the language your next professional moment needs.'],
  ['Study & academic', 'Build the English your next opportunity asks for.', 'Read deeper, write with control and make your ideas easier to follow.'],
  ['Exams+', 'Add targeted preparation when a score matters.', 'IELTS, TOEFL, SAT, YDT, YDS and YÖKDİL support when a deadline calls for it.'],
];

const familyPanel = (family) => {
  const meta = familyMeta[family];
  const links = familyExamIds(family).map((id) => {
    const exam = exams[id];
    return `<a href="${exam.route}" class="group rounded-2xl border border-ink/15 bg-white/70 p-5 transition-transform motion-safe:hover:-translate-y-1"><span class="flex items-center justify-between"><strong class="text-lg">${exam.label}</strong><span class="grid h-9 w-9 place-items-center rounded-full bg-ink text-white">${icon('arrow','h-4 w-4')}</span></span><span class="mt-3 block text-sm leading-6 text-muted">${exam.skills}</span></a>`;
  }).join('');
  const heading = family === 'international' ? 'English that travels. Exam advantage when it counts.' : 'English for your next chapter in Türkiye.';
  return `<article class="rounded-[2rem] ${meta.color === 'lime' ? 'bg-lime/70' : 'bg-lilac/85'} p-6 sm:p-8"><div class="flex items-center justify-between gap-4"><p class="${s.eyebrow}">${meta.label}</p><a class="text-xs font-bold underline underline-offset-4" href="${meta.route}">See the full comparison</a></div><h2 class="mt-6 max-w-lg text-3xl font-semibold tracking-tight sm:text-4xl">${heading}</h2><p class="mt-4 max-w-xl text-sm leading-7 text-muted">${meta.intro}</p><div class="mt-7 grid gap-3">${links}</div></article>`;
};

const offerCards = () => homeServices.map(([title, headline, copy], i) => `<article class="rounded-2xl border border-line bg-white p-6 sm:p-7"><div class="flex items-center justify-between gap-3"><span class="text-xs font-bold text-muted">0${i + 1}</span><span class="rounded-full bg-paper px-3 py-1.5 text-[10px] font-bold uppercase tracking-widest">${i === 3 ? 'Exams+' : 'English'}</span></div><h3 class="mt-7 text-xl font-semibold tracking-tight">${title}</h3><p class="mt-3 text-lg font-medium leading-7">${headline}</p><p class="mt-3 text-sm leading-7 text-muted">${copy}</p></article>`).join('');

export { familyPanel };

export const home = () => ({
  page: 'index',
  title: 'English that moves you forward | Exams+ when you need it',
  description: 'Build practical English for everyday life, work and study, then add IELTS, TOEFL, SAT, YDT, YDS or YÖKDİL preparation when a score matters.',
  body: `<section id="top" class="${s.wrap} grid gap-10 pb-14 pt-14 lg:grid-cols-[1.08fr_.92fr] lg:items-start lg:gap-14 lg:pt-20"><div><p class="${s.eyebrow} mb-6 flex items-center gap-2.5 text-muted"><span class="h-2 w-2 rounded-full bg-ink"></span>English for where life takes you</p><h1 class="max-w-3xl text-[clamp(3.1rem,6.5vw,6.7rem)] font-semibold leading-[.98] tracking-[-.075em]">Build the English<br>that moves you forward.<br><span class="relative inline-block">Exams+ when you need the score.</span></h1><p class="mt-8 max-w-xl text-base leading-8 text-muted">From everyday confidence to ambitious academic goals, get practical English coaching that fits the way you work. Add an exam route when a deadline, application or score makes it useful.</p><div class="mt-8 flex flex-wrap gap-3">${button('Help me choose','data-open="finder"')}${link('See the free library','resources.html',true)}</div></div><div class="rounded-[2rem] bg-ink p-6 text-white sm:p-8"><div class="flex items-start justify-between gap-4"><p class="${s.eyebrow} text-lime">One English studio</p><span class="rounded-full border border-lime/50 px-3 py-1.5 text-[10px] font-bold uppercase tracking-widest text-lime">Exams+</span></div><p class="mt-6 max-w-xl text-2xl font-semibold leading-tight">Build the language first. Add the right advantage when your next move needs a score.</p><div class="mt-8 grid gap-3"><div class="rounded-2xl bg-white/10 p-4"><p class="text-xs font-bold uppercase tracking-widest text-lime">01 / English every day</p><p class="mt-2 text-sm leading-6 text-white/75">Conversation, work, travel and study support that stays useful after the lesson.</p></div><div class="rounded-2xl bg-white/10 p-4"><p class="text-xs font-bold uppercase tracking-widest text-lime">02 / Exams+ on demand</p><p class="mt-2 text-sm leading-6 text-white/75">Focused IELTS, TOEFL, SAT, YDT, YDS or YÖKDİL preparation when the outcome matters.</p></div></div><div class="mt-8 grid grid-cols-3 gap-3 border-t border-white/20 pt-6 text-xs"><span><strong class="block text-2xl text-lime">04</strong>English paths</span><span><strong class="block text-2xl text-lime">06</strong>exam routes</span><span><strong class="block text-2xl text-lime">01</strong>clear plan</span></div></div></section><section id="english-offers" class="${s.wrap} pb-16"><div class="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end">${eyebrow('One language. Many reasons to learn.')}<p class="max-w-md text-sm leading-7 text-muted">Choose the outcome you care about today. Your route can grow with you as life, work or study changes.</p></div><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">${offerCards()}</div></section><section id="exam-routes" class="${s.wrap} grid gap-5 pb-16 lg:grid-cols-2">${familyPanel('international')}${familyPanel('turkiye')}</section><section class="${s.wrap} pb-20"><div class="grid gap-8 overflow-hidden rounded-[2rem] bg-[#e6e9dc] p-4 sm:grid-cols-[1fr_.85fr] sm:items-end sm:p-5"><img src="assets/hero-coaching.jpg" alt="An online English coaching conversation" width="1536" height="1024" class="h-64 w-full rounded-[1.4rem] object-cover sm:h-80"><div class="p-4 sm:p-7"><p class="${s.eyebrow} text-muted">Coaching that keeps up with real life</p><h2 class="mt-5 text-3xl font-semibold leading-tight tracking-tight">Your English should work outside the lesson.</h2><p class="mt-4 text-sm leading-7 text-muted">Bring the meeting, the move, the application or the conversation you care about. We turn it into practice you can use immediately.</p>${button('Build my coaching brief','data-open="consultation"',true)}</div></div></section><section class="bg-white" id="how-it-works"><div class="${s.wrap} grid gap-10 py-20 lg:grid-cols-[.72fr_1.28fr]"><div>${eyebrow('A better English week starts here')}<h2 class="${s.heading}">Know what to practise next.</h2><p class="mt-5 text-sm leading-7 text-muted">Choose the goal, try the method, save what works and build a week you can actually keep.</p>${button('Build my study plan','data-open="planner"',true)}</div><div class="grid gap-5 sm:grid-cols-3">${[['Find your focus','Name the moment or skill you want to handle better.'],['Practise the real moment','Use a short, original activity with immediate feedback.'],['Carry it into life','Save one useful action and repeat it this week.']].map(([title, copy], i) => `<article class="border-t-2 border-ink pt-5"><span class="text-xs font-bold text-muted">0${i + 1}</span><h3 class="mt-5 text-xl font-semibold">${title}</h3><p class="mt-3 text-sm leading-7 text-muted">${copy}</p></article>`).join('')}</div></div></section><section id="formats" class="${s.wrap} py-20 sm:py-24">${eyebrow('Choose your level of support')}<h2 class="${s.heading} mt-5">The right structure makes progress easier.</h2><div class="mt-10 grid gap-4 md:grid-cols-3">${[['Private 1:1','A flexible pace, direct feedback and room to improve.'],['VIP group of 3–5','Shared momentum with practice built around your level.'],['Exam consultancy','A focused route when a deadline or score matters.']].map(([title, copy], i) => `<article class="rounded-2xl border border-line p-7"><p class="text-xs font-bold text-muted">0${i + 1}</p><h3 class="mt-6 text-xl font-semibold">${title}</h3><p class="my-4 text-sm leading-7 text-muted">${copy}</p>${button('Build my coaching brief',`data-open="consultation" data-format="${title}"`,true)}</article>`).join('')}</div></section><section id="resources" class="${s.wrap} pb-20"><div class="grid gap-8 rounded-[2rem] bg-[#e6e9dc] p-7 sm:p-10 lg:grid-cols-[.85fr_1.15fr] lg:items-end"><div>${eyebrow('Start with something useful today')}<h2 class="${s.heading} mt-5">Small tools.<br>Bigger momentum.</h2><p class="mt-5 max-w-md text-sm leading-7 text-muted">Use a free checklist, planner or practice log for the goal in front of you. Keep the useful parts and come back when you need them.</p>${link('Open the free library','resources.html',true)}</div><div class="grid gap-3 sm:grid-cols-2">${[['TOEFL iBT 2026+','Email checklist','toefl-notes'],['YDT','Net takip çizelgesi','ydt-net'],['YDS · YÖKDİL','Vocabulary error log','yds-log'],['SAT','Reading & Writing pacing','sat-pacing']].map(([label, title, id]) => `<a href="resources.html?resource=${id}" class="rounded-xl bg-white/75 p-5"><span class="text-[10px] font-bold tracking-widest text-muted">${label}</span><strong class="mt-2 block text-sm">${title} ${icon('diagonal','inline h-4 w-4')}</strong></a>`).join('')}</div></div></section><section id="faq" class="${s.wrap} grid gap-10 pb-24 md:grid-cols-[.72fr_1.28fr]"><div>${eyebrow('Before you begin')}<h2 class="${s.heading} mt-5">Good questions.<br>Make a confident choice.</h2></div><div>${[['Can I learn English without preparing for an exam?','Absolutely. Everyday, professional and academic English are the core of the studio. Exams+ is an optional route you can add when a score, deadline or application makes it useful.'],['When should I add an exam route?','Start with the requirement and the date in front of you. Add IELTS, TOEFL, SAT, YDT, YDS or YÖKDİL support when you need focused practice for that outcome.'],['Is SAT an English test or a college-readiness test?','No. The full SAT includes Math and Reading & Writing. This studio’s SAT coaching covers Reading & Writing only.'],['Are these official questions or scores?','No. All starters are original practice. Feedback describes the activity you completed and does not predict an official score.'],['Can I get a plan made for me?','Yes. Choose private 1:1, a VIP group of 3–5 or exam consultancy, then prepare a brief with your goal and timeline.']].map(([q, a]) => `<details class="border-b border-line py-5"><summary class="cursor-pointer pr-5 text-sm font-bold leading-6">${q}</summary><p class="mt-4 pr-4 text-sm leading-7 text-muted">${a}</p></details>`).join('')}</div></section>`
});
